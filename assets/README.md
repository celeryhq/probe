# Test Assets

## Test Assets

Place files here for scenarios that need uploads (product photos, logos, documents).

Reference in eval config:
```yaml
assets:
  product_photo: "assets/sample-product.jpg"
  logo: "assets/sample-logo.png"
```

Scenarios reference via `{{asset:product_photo}}`.
